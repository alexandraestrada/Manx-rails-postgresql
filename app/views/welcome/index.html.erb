<h1>meggings</h1>


<%if current_user %>
			
	<%= link_to 'Sign Out', new_auth_path, method: :delete %>

	<%= current_user ? "Hello "+ current_user.first_name.capitalize + "!": "" %>			

<%else%>
		<p><%= link_to 'Sign in',new_auth_path, url:"#login" %>/<%= link_to 'Sign Up', new_user_path %></p>
			
<%end%>
